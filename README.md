To run a notebook locally you need a GPU which supports CUDA.

Furthermore you need:
- Python (or a Python distribution like Anaconda)
- CUDA
- PyTorch
- Jupyter Lab

Alternatively you can use a hosted environment like Google colab.
